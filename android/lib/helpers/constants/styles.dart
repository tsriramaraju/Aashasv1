import 'package:flutter/material.dart';

//region WelcomeScreen

const KInputBox = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.all(
    Radius.circular(15),
  ),
  boxShadow: [
    BoxShadow(
      color: Colors.black12,
      blurRadius: 5.0,
    )
  ],
);

//endregion
